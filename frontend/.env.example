# Base URL of your existing backend API
NEXT_PUBLIC_API_URL=http://localhost:4000

# Name of the HTTP-only cookie your backend sets on login.
# Used by middleware.ts to decide whether a request is authenticated.
AUTH_COOKIE_NAME=token
